# 🚀 Dinamik İstasyon - Proje İlerleme Durumu (PROGRESS.md)

Bu dosya, projenin genel vizyonunu, tamamlanan özellikleri ve gelecekteki planları takip etmek için kullanılmaktadır. (SSOT Kurallarına göre güncellenir).

## 🎯 Proje Vizyonu
İlkokul/Ortaokul öğrencileri için matematik öğrenimini sıkıcı bir görev olmaktan çıkarıp, **"Uzay / Bilim-Kurgu"** temalı, yüksek kaliteli, animasyonlu ve ödül sistemli (Atom kazanma) bir oyun deneyimine dönüştürmek.

---

## 🏗️ Altyapı ve Vibe Mimarisi (Vibe Coder Kit)
- [x] **Vibe Root Sistem** (`AGENTS.md`): Sistem tamamen Vibe Coder metoduna göre modüler hale getirildi.
- [x] **SSOT Tabanı:** `MEB_ATOMLARI.md` ve `MODULES.md` dosyalarıyla projenin şaşmaz haritaları kuruldu.
- [x] **Kural Setleri:** `typescript-react.md` ve `ui-ux-design.md` standartları aktive edildi.
- [x] **Akıllı Skiller:** Prompt Enhancer, Module Builder, Code Reviewer ve Refactor Master yetenekleri Ajan kütüphanesine katıldı.

---

## ✅ Tamamlanan Modüller (Done)

### 🌍 5. Sınıf Uzay Laboratuvarı
- [x] **Devasa Nicelik Çekirdeği (Magnitude Core):** Milyonlu sayıların (Trilyonlara kadar) okunup yazılmasını sağlayan ve gezegen kalkanı enerjisi yükleten modül (MAT.5.1.1.1, MAT.5.1.1.2).
- [x] **Kesirler Senkronizatörü (Fraction Synchronizer):** Kesirleri genişletme, bileşik/tam sayılı çevirimi ve veri hizalaması yaptıran modern Neon oyun modülü (MAT.5.1.3, MAT.5.1.4). Bot diyalogları ve HUD ekranları tam entegre çalışıyor.

### 🧩 Geometri & Fizik Motoru
- [x] **3D Geometri Laboratuvarı:** Three.js destekli, küp, silindir, prizma vb. şekillerin açınımı ve KaTeX ile formül simülasyonları.
- [x] **Mutlak Değer Lazerleri:** Aynalı sistemlerle 0'a olan uzaklığı (mutlak değeri) anlatan interaktif deney.
- [x] **Boyut Odası:** Uzunluk (x), Alan (x²), Hacim (x³) 2D/3D CSS geçişleri.
- [x] **Pisagor Sıvı İspatı:** Vektörel (SVG) pürüzsüz animasyonlu sıvı aktarımı ile dik üçgen oranı kanıtı.

### 🧮 5. Sınıf Cebir (Algebra) Laboratuvarı
- [x] **Sıfır Motoru & Kapsül Avı (Zero Engine):** 10, 100, 1000 ve katlarıyla zihinden kısa yoldan çarpma işlemlerini 'Hata Ayıklama (Bug Hunt)' temasıyla anlatan, sürükle-bırak animasyonlu interaktif laboratuvar modülü (MAT.5.1.2.a).

### 📐 6. Sınıf Geometri Laboratuvarı
- [x] **Alan ve Pi Laboratuvarı (Area & Pi Lab):** Dikdörtgen, Üçgen ve Paralelkenar'ın (matematiksel ve görsel tutarlılıkla animasyonlu "kes-yapıştır" metotlarıyla) alan bağıntılarını anlatan dinamik alanlar. Çemberin çevresi ile çapı arasındaki $\pi$ bağıntısını makaraya sarılan neon iplerle kanıtlayan 2. sekme uygulaması (MAT.6.3.3.1 - MAT.6.3.4.1). *Vibe Coder onarımıyla root scroll `min-h-screen` hatası düzeltildi, Dashboard kategori bağımlılığı (`gradeRange`) kesin sınıf bazlı sisteme çekildi.*

### 🧮 9. Sınıf Cebir (Algebra) Laboratuvarı
- [x] **Denklem Denizaltısı (Equation Submarine):** Doğrusal denklem sistemlerinin (İki bilinmeyenli) çözümünü, denizaltının reaktörü ve oksijen sistemini onarma metaforu eşliğinde önce Taraf Tarafa Toplama, ardından Katsayı Dengeleme aşamalarıyla anlatan ve yerine koyma metodunu pekiştiren ileri seviye simülasyon (MAT.9.2.3.1).
- [x] **Kök ve Üs Reaktörü:** Üslü/köklü ifadeler, sayı kümeleri, aralık kapıları ve işlem özelliklerini tek reaktör panelinde sadeleştiren 9. sınıf sayı laboratuvarı (MAT.9.1.1.x - MAT.9.1.5.x).
- [x] **Fonksiyonel Hologram Odası:** f(x)=x referans lazerini eğim, yatay/dikey kaydırma, mutlak değer aynası ve eşitsizlik alanı üzerinden görselleştiren fonksiyon simülasyonu (MAT.9.2.1.x, MAT.9.2.2.x, MAT.9.2.3.2).
- [x] **Akıllı Mantık Devreleri:** Algoritmik akış, VE/VEYA/İSE/YA DA kapıları ve her/bazı niceleyicilerini 0/1 devre tahtasında test eden mantık laboratuvarı (MAT.9.3.x).
- [x] **Üçgen Gerilim Laboratuvarı:** Açı-kenar ilişkisi, benzerlik oranları ve Tales/Öklid/Pisagor kilitlerini canlı SVG üçgen üzerinde çalıştıran geometri modülü (MAT.9.4.1.x, MAT.9.5.2.x, MAT.9.5.3.x).
- [x] **Dönüşüm Adli Bilişimi:** Döndürülmüş/yansıtılmış şekillerde dönüşüm merkezi, açı imzası ve ayna eksenini analiz ettiren dönüşüm modülü (MAT.9.5.1.x).
- [x] **Veri ve Olasılık Radarı:** Standart sapma, kutu-bıyık medyanı, gözlemsel olasılık ve tümevarım tahminini tek radar arayüzünde birleştiren veri modülü (MAT.9.6.x, MAT.9.7.x).

### 🎮 İlkokul Odaklı Modüller (Eski Sürümler)
- [x] **Sayı Doğrusu Zıplaması:** Toplama ve Çıkarma için neon uzay ekseni.
- [x] **Onluk Bozma Fabrikası:** Fiber tüplerde Onluk ve Birlik laboratuvarı.

---

## 🚧 Üzerinde Çalışılanlar & Gündem (In Progress)
- [x] **Öğrenci Bilgi/Kazanım Kartı (Dashboard UI):** Kullanıcının adını görebileceği, biriktirdiği kazanım atomlarını ve uzay rütbesini "Glassmorphism" stiliyle görebileceği Profil Arayüzü (Ana giriş ekranı olan Dashboard ile harmanlandı).
- [x] **Vektörel Tasarım Paneli (Işın, Doğru, Açı Fiziği):** Lazer tabanlı doğru çizim, ters açı tespiti ve sanal iletki kullanımı. (MAT.5.3.x grubunu kapsar).
- [x] **Poligon Çatışma Testi (Üçgen Eşitsizliği):** Üçgenin kenar uzunluklarını slider ile ayarlayarak oluşup oluşamama durumunu (Üçgen Eşitsizliği - MAT.5.3.7.1) fiziksel olarak simüle eden laboratuvar. *Vibe Coder incelemesiyle `GameHeader` entegre edildi, AstroBot ipucu akışı (timeout debounce) mükemmelleştirildi. `Math.atan2` tabanlı kolların hatalı açı problemleri onarılarak mühendislik stili dashed-line eklentileri (U AÇIKLIK) yapıldı. Orantısız kenarlarda yaşanan SVG viewport clipping/taşıma sorunları (scale küçültmesiyle) engellendi. CSS transform-origin bazlı çubuk kopma (rotation) sorunları giderilerek direkt koordinat hesaplamalı (`x1, y1`) yaylı (`spring`) sisteme geçildi.*
- [x] **Kod Refactoring (Kısmi):** Ortak UI bileşenleri (`AstroBot`, `GlobalAstroBot`, `Protractor`, `MeasurementPanel`) bağımsızlaştırılarak merkezileştirildi.
- [x] **Codex Devralma ve Roadmap Senkronizasyonu:** Tüm proje dokümanları incelendi, `docs/ROADMAP.md` teknik omurga + publish hattı + müfredat sprintleri olarak yeniden düzenlendi. Sıradaki önerilen modül 6. sınıf **Kuantum Filtre İstasyonu** olarak belirlendi.
- [x] **9. Sınıf Lab Paketi v1:** Ortak `Grade9LabShell` altyapısı kuruldu; 6 yeni 9. sınıf modülü registry ve dashboard'a bağlandı. Browser Use ile `?qa=1` güvenli test oturumunda 6/6 yeni modülün tüm görev zincirleri uçtan uca tamamlandı. AstroBot'un kontrol butonlarını tıklanamaz hale getirme riski, Kök/Üs test id çakışması, Üçgen açı slider test/UX eksiği ve veri radarı 0 deney yayı düzeltildi. Son görsel denetimde orta genişlikte yatay sahne sıkışması giderildi, görev zinciri kompakt yatay akışa alındı ve Fonksiyon stepper hızlı tıklama state hatası fonksiyonel update ile onarıldı.
- [x] **9. Sınıf Lab Paketi v2 Görsel Ayrıştırma:** `Grade9LabShell` ortak görev paneli dayatmasından arındırıldı; sadece GameHeader, tamamlanma ve görev state iskeletini taşıyor. Modüllere Hologram/Reaktör/Devre/Gerilim/Adli Analiz/Radar brifingleri, ayrı arka plan kimlikleri ve tema bazlı kontrol varyantları eklendi. Browser Use ile 6/6 modül tekrar tamamlandı.
- [x] **9. Sınıf Lab Paketi v2.1 Deneyim Derinleştirme:** Altı modülün ana sahnesi aktif göreve göre matematiksel metaforu daha açık gösterecek şekilde güçlendirildi: reaktör kesiti, mutlak değer ayna çizgisi, İSE 1=>0 devre hatası, üçgen ölçü/benzerlik/teorem overlay'leri, dönüşüm hedef iz-hayalet çakıştırması ve olasılık 1000 atış projeksiyonu eklendi. Browser Use ile 6/6 modülün tamamlanma zinciri ve yeni görsel işaretleri doğrulandı.
- [x] **9. Sınıf Lab Paketi v3 Sıfırdan Deneyim Rebuild:** Kullanıcı geri bildirimiyle 6 modül "aynı laboratuvar panelinin farklı konusu" hissinden çıkarıldı. `radical-power-reactor`, `statistics-probability-radar`, `triangle-tension-lab`, `logic-circuit-lab`, `function-hologram-room` ve `transformation-forensics` tek ana deney + aktif görev kontrolü yaklaşımıyla yeniden yazıldı. Eski ana `ChoiceButton`/`MetricPill` UI kalıpları kaldırıldı; sahnelere kalıcı `data-testid` markerları eklendi. Browser Use in-app QA ile 6/6 modülün tüm görev zincirleri tamamlandı; final konsol turunda yeni warning/error görülmedi. `npm run build` temiz geçti.
- [x] **Triangle Tension Lab v4 Makro Atom Pilot:** Kullanıcıyla birlikte üçgen modülü makro atom mantığına indirildi. `triangle-tension-lab` artık yalnız `MAT.9.4.1.x` kapsamını tamamlıyor: köşe sürüklemeli açı-kenar ilişkisi ve ayrı "Açı Açma Masası"nda açı token'larını 180° ispat rayına taşıma. Registry atomları iki atoma düşürüldü, `docs/MODULES.md` üçgen kapsamı Triangle / Similarity / Theorem makro ayrımına çekildi. Browser Use in-app QA ile C köşesi gerçek sürüklendi, açı token'ları rayda tamamlandı, completion ekranı görüldü ve taze konsol turu temiz kaldı. `npm run build` geçti.
- [x] **Kök/Üs ve Veri Makro Rebuild v4:** Triangle v4 kalite çıtası iki modüle daha taşındı. `radical-power-reactor` yalnız `MAT.9.1.1.x` / `MAT.9.1.2.x` odağına indirildi; üslü çekirdek füzyonu ve kökten tam kare ayırma sürüklemeli iki deney oldu. `statistics-probability-radar` **Veri Karar Radarı**na dönüştü; yalnız `MAT.9.6.x` odağıyla sigma tarayıcısı ve kutu-bıyık medyan okuyucusu kullanıyor. Aralıklar, sayı kümeleri/işlem yasaları ve olasılık ayrı gelecek makro modül olarak `docs/MODULES.md` içine ayrıldı. `npm run build` geçti; Browser Use in-app QA ile iki modülün sürükleme zinciri, completion ekranı ve taze konsol turu doğrulandı.
- [x] **Kök ve Üs Reaktörü v5 Baştan Tasarım:** `radical-power-reactor` Apple Education hissine daha yakın, sakin glass/spotlight sahneye taşındı. Üs tarafında `2³` ve `2²` enerji çekirdekleri merkez füzyon yuvalarında birleşip `2⁵ = 32` sonucunu üretiyor; kök tarafında `√72` kristali `√36 -> 6` dış haznesi ve `√2` kök haznesi olarak ayrılıyor. Yuvalar büyük dokunma alanlarına dönüştürüldü, orta genişlikte yatay kırpılma giderildi. Browser Use ile yanlış onaylar, iki görev, completion ekranı ve taze console delta 0 doğrulandı. `npm run build` geçti.
- [x] **Kök ve Üs Reaktörü v5.1 Görsel Hata Onarımı:** Browser Use görsel turunda kök sahnesindeki `motion.rect` kaynaklı hatalı token zemini yakalandı; `√36` ve `√2` kart arka planları SVG altına kayıp anlamsız renkli blok gibi görünüyordu. SVG `y` hareketi sabit konum + opacity/scale nabzına çevrildi. Orta viewport layout'u da sıkılaştırıldı; sağ kontrol paneli ve `Deneyi Onayla / Sıfırla` aksiyonları ilk ekranda görünür kaldı. `npm run build`, `git diff --check` ve Browser Use final e2e temiz geçti.
- [x] **Fonksiyonel Hologram v4 Gemini Eleştiri Rebuild:** Gemini dış eleştirisi `gemini-3-flash-preview` ile alındı; ana riskin çok kapsamı tek modüle sıkıştırmak olduğu doğrulandı. `function-hologram-room` yalnız `MAT.9.2.1.x` doğrusal fonksiyon dönüşümlerine indirildi. Mutlak değer ayna grafiği ve eşitsizlik alanı `docs/MODULES.md` içinde ayrı gelecek makro modüllere ayrıldı. Yeni deneyde öğrenci mavi kaynak düğümüyle `r/k` kaydırmasını, yeşil eğim koluyla `a` eğimini doğrudan manipüle ediyor; dört kalibrasyon görevi Browser Use ile gerçek sürüklemeyle completion ekranına ulaştı. `npm run build` geçti.
- [x] **Mantık Devreleri v4 Kapalı Çevrim Rebuild:** `logic-circuit-lab` yalnız `MAT.9.3.2.x` kapı atomlarına indirildi. Algoritma akış şeması ve niceleyiciler `docs/MODULES.md` içinde ayrı gelecek makro modüllere ayrıldı. Yeni devrede tek oyuncak A/B anahtarları; aktif kapı kartuşu sırayla VE, VEYA, İSE ve YA DA olarak değişiyor. Browser Use ile 4/4 görev completion ekranına ulaştı. Gemini 3 Flash'a ekran görüntüsü gönderildi; giriş/çıkış ayrımı, mini doğruluk tablosu sadeleştirme ve 0 değerinin gri/sönük gösterilmesi geri bildirimi alındı ve uygulandı. `npm run build` geçti.
- [x] **Dönüşüm Adli Bilişimi v4 Doğrudan Manipülasyon:** `transformation-forensics` slider/otomatik hedef butonlarından arındırıldı. Öğrenci döndürme merkezini doğrudan sahnede orijine sürüklüyor, açı halkasını 90° hedef izine çeviriyor ve yansıma eksenini çizgiler üzerinden seçiyor. İlk görevdeki gereksiz açı halkası gizlenerek "neyle oynayacağım?" sorusu merkez handle'a indirildi. Browser Use ile 3/3 görev zinciri ve completion ekranı doğrulandı; taze konsol turunda yeni warning/error yoktu. `npm run build` geçti.
- [x] **Dönüşüm Adli Bilişimi v4.1 E2E Sağlamlaştırma:** Superpowers doğrulama turunda dev server/origin farkı ve SVG eksen seçimi ayrı ayrı test edildi. Y ekseni seçiminde `data-testid` tıklaması eksen kesişimine düştüğü için sahneye her eksen için ayrı uç seçim düğümü eklendi. Browser Use ile yanlış onay hata mesajları, merkez sürükleme, açı çevirme, Y ekseni seçimi ve completion ekranı tekrar doğrulandı; taze console turu temiz kaldı.
- [x] **Dönüşüm Adli Bilişimi v5 Görsel Cila:** Modül Apple Education benzeri daha sakin premium glass yüzeylere, düşük gürültülü grid/spotlight sahneye ve okunabilir orijinal-hedef-hayalet ayrımına taşındı. Orijin hedefi, 90° hedefi ve eksen uç düğümleri dokunulabilir yapıldı; böylece hem tablet kullanımı hem Browser Use QA güvenilirliği arttı. Browser Use ile yanlış onaylar, hedef kilitleme, Y ekseni seçimi ve completion ekranı tekrar doğrulandı. `npm run build` geçti.
- [x] **9. Sınıf Görsel Uçtan Uca QA Standardı:** Kullanıcının "uçtan uca test et" beklentisi proje skill'i olarak kaydedildi (`project-visual-e2e-qa`). Browser Use in-app QA ile `radical-power-reactor`, `function-hologram-room`, `equation-submarine`, `logic-circuit-lab`, `triangle-tension-lab`, `transformation-forensics` ve `statistics-probability-radar` yanlış deneme, doğru etkileşim, görev geçişi ve completion ekranı üzerinden tek tek doğrulandı. Orta viewport'ta sağ panel/aksiyon kırpılmalarını azaltmak için 9. sınıf kontrol panelleri kompakt hale getirildi; AstroBot mesajları kalıcı şekilde sahneyi örtmesin diye otomatik kapanma aldı. `npm run build` ve `git diff --check` geçti; console'da kalan uyarılar Firestore WebChannel bağlantı uyarılarıyla sınırlı.
- [x] **Ürün Tercihi ve Memory Sync:** Kullanıcının tasarım tercihleri kalıcı proje hafızasına işlendi. 3D silindir/prizma modüllerindeki açma-kapama, döndürme, zoom ve dokunulabilir açınım hissi ile 1. sınıf modüllerindeki sade, oyunlu ve hemen anlaşılır akış bundan sonraki modül tasarımlarının kalite referansı kabul edildi. `docs/MODULE_DESIGN_GUIDE.md` eklendi; `docs/MODULES.md` içine "tek ana oyuncak", anlamlı görsel, özgün modül kimliği ve geniş üniteleri ayrı makro deneylere bölme ilkeleri yazıldı.
- [x] **10-11 Uzun Geliştirme Kuyruğu:** Saatlik otomasyonun da takip edeceği `docs/DEVELOPMENT_QUEUE_10_11.md` eklendi. 10. sınıf için 18, 11. sınıf için 15 dar makro deney tasarlandı; her biri SSOT atomları, ana oyuncak ve öncelik gerekçesiyle yazıldı. `docs/MODULE_DONE_CRITERIA.md` modülün bitmiş sayılması için build, Browser Use görsel QA, yanlış/doğru akış, completion, console ve responsive kontrollerini sabitledi. `.agent/WORKLOG.md` uzun geliştirme için teknik oturum günlüğü olarak eklendi.
- [x] **Otonom Modül Fabrikası v1:** İnsan bağımlılığını azaltmak için `docs/AUTONOMOUS_MODULE_PIPELINE.md` eklendi. Üretim hattı `Ready -> Spec Draft -> Implementing -> Static Verified -> Browser Use QA -> Gemini Critique -> Done` state machine'iyle tanımlandı. `npm run module:check -- <module-id>` guard komutu eklendi; SSOT atom, spec, queue, registry, route, test-id, `any` kullanımı, `HighSchoolLabShell` ve klavye/Home QA fallback kontratlarını statik olarak denetliyor. Browser QA'da Browser Use IAB dışına düşmeme kuralı korunuyor; IAB/dev server/stale tab/screenshot timeout gibi iç operasyon takılmalarında ajan kendi recovery merdivenini uyguluyor. Gemini 3 Flash must-fix kapısı ve `docs/MODULE_QUALITY_SCORECARD.md` puan defteri modül bitiş kriterine eklendi. Aktif uzun koşu sınıf kilidi 11. sınıftır.
- [x] **11. Sınıf Çokgen Köşegen ve Simetri Atölyesi:** `MAT.11.2.4.x` kapsamı tek ana çokgen kalibrasyon masası olarak tamamlandı. Öğrenci n=6 hedefini, köşegen lazer sayımını, dış açı 360° yürüyüşünü ve düzgün altıgen simetri eksenlerini aynı sahnede manipüle ediyor. Browser Use ile yanlış onay, `Home` fallback üzerinden üç görev, completion ve console `[]` geçti. Gemini 3 Flash ilk turda alt ray sıkışması ve hedef dili must-fix verdi; raylar ferahlatıldı, değerler ray sağına alındı ve final Gemini 98/100 PASS verdi. `npm run module:check`, `npm run build` ve `git diff --check` temiz.
- [x] **10. Sınıf Fonksiyon Mu Filtresi:** `MAT.10.2.1.x` kapsamı tek ana oyuncak olarak tamamlandı. Dikey lazer filtresi, aynı x değerinin birden fazla y üretmesi durumunda fonksiyon şartını kırmızı alarm olarak gösteriyor; son görevde `f(x)=2x+1` kuralı tanım kümesi girişlerini çıkış portlarına bağlatıyor. Tarayıcı sürüklenebilir ve klavye ile erişilebilir; portlar tıklama/klavye seçimine açık. Browser Use ile yanlış onay, iki tarama görevi, port eşleştirme, completion DOM doğrulaması ve console warning/error kontrolü geçti; `npm run build` geçti.
- [x] **10. Sınıf Parabol Şekil Stüdyosu:** İlk 10. sınıf modülü tamamlandı. `MAT.10.2.2.x` kapsamı tek ana oyuncak olarak ele alındı: mavi tepe düğümü `h/k` kaymasını, pembe genişlik kolu `a` katsayısını doğrudan manipüle ediyor. `HighSchoolLabShell` eklendi, route `/embed/algebra/parabola-shape-studio` olarak registry'ye bağlandı. Browser Use ile yanlış onay, 4 sürüklemeli görev, completion DOM doğrulaması ve console warning/error kontrolü geçti; `npm run build` ve `git diff --check` temiz.
- [x] **10. Sınıf Tanım Kümesi Kapıları:** `MAT.10.2.3.x` ve `MAT.10.2.4.x` kapsamı ayrı makro deney olarak tamamlandı. Karekök başlangıcı/güvenli bölge ve rasyonel asimptot/payda sıfır alarmı tek koordinat kapı sahnesinde işleniyor; öğrenci x=0 hedef çizgisine sürükleyerek tanım bölgesi yasağını görür. Handle'lar klavye ile de erişilebilir hale getirildi: sağ/sol oklar ince ayar, `Home` orijin hizalama sağlıyor. Browser Use ile yanlış onay, 4 görev zinciri, completion DOM doğrulaması ve console warning/error kontrolü geçti; `npm run build` ve `git diff --check` temiz. Screenshot yakalama Browser Use CDP timeout verdiği için DOM/etkileşim/console kanıtıyla ilerlenip başka ekran aracına geçilmedi.
- [x] **10. Sınıf Trigonometrik Teodolit:** `MAT.10.4.1.x` kapsamı ayrı makro deney olarak tamamlandı. Dik üçgen sahnesinde sarı teodolit kolu, karşı/komşu/hipotenüs kenarları ve oran okuyucu aynı ana oyuncakta birleşiyor; öğrenci sin, cos, tan ve cot için önce pay, sonra payda kenarını seçerek oranı kuruyor. Browser Use QA sırasında hipotenüs tıklamasını engelleyen açı kolu katman çakışması yakalandı ve düzeltildi; sahne küçük viewport için kompakt hale getirildi. Yanlış deneme, dört oran zinciri, completion ekranı ve console warning/error kontrolü Browser Use ile geçti. `npm run build` ve `git diff --check` temiz.
- [x] **10. Sınıf Analitik Rota Haritası:** `MAT.10.5.1.x` ve `MAT.10.5.2.x` kapsamı tek navigasyon oyuncağı olarak tamamlandı. A/B istasyonları koordinat düzleminde sürükleniyor; yeşil `Δx`, mavi `Δy`, sarı mesafe kablosu, canlı eğim ve nokta-eğim denklemi aynı sahnede okunuyor. Son görevde mor transfer düğümü yalnız içten bölme katmanında açılıyor ve `AP:PB = 1:3` oranına kilitleniyor. Browser Use ile yanlış onay, dört görev zinciri, transfer düğümü görünürlüğü, completion ekranı, screenshot smoke ve console warning/error kontrolü geçti. `npm run build` ve `git diff --check` temiz.
- [x] **11. Sınıf Trigonometrik Osiloskop:** `MAT.11.1.1.1` ve `MAT.11.1.1.2` kapsamı ayrı makro deney olarak tamamlandı. Birim çember faz kolu sinüs/kosinüs dalgasını dijital osiloskop ekranına çiziyor; öğrenci 90°, 180° ve 360° hedeflerinde dikey/yatay izdüşümün dalga değerini doğrudan görüyor. Browser Use ile yanlış onay, dört faz görevi, completion ekranı, mobil/dar viewport completion başlığı ve console warning/error kontrolü geçti. `ModuleCompletedScreen` başlığı dar embed ekranlarda kırpılmayacak şekilde responsive hale getirildi. `npm run build` ve `git diff --check` temiz.
- [x] **11. Sınıf Tanjant Asimptot Kapıları:** `MAT.11.1.1.3` ve `MAT.11.1.1.4` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci 0°-360° grafik tünelinde neon asimptot kapılarını sürükleyerek `tan(x)` için 90°/270° duvarlarını, `cot(x)` için 0°/180°/360° duvarlarını kilitliyor; son görevde tarayıcı 180° duvarına yaklaşınca değerin büyüdüğünü ama duvar üzerinde nokta oluşmadığını görüyor. Browser Use ile yanlış deneme, dört görev, completion, console warning/error ve Dashboard 11. sınıf `2 GÖREV` sayımı doğrulandı. `npm run build` ve `git diff --check` temiz.
- [x] **11. Sınıf Üstel Büyüme Reaktörü:** `MAT.11.1.3.1` ve `MAT.11.1.3.2` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci tek taban çekirdeğini `a=1` nötr kapısının sağına veya soluna sürüklüyor; sahne `a>1` durumunda artan, `0<a<1` durumunda azalan üstel grafiği canlı gösteriyor. Browser Use ile görsel smoke, yanlış onay AstroBot hatası, gerçek CUA sürükleme, `Home` ile iki görev hizalama, completion ekranı ve console warning/error kontrolü geçti. İlk QA turunda görev 2 başlarken artan grafiğin ekranda kalması kafa karıştırıcı bulundu; görev geçişinde taban nötr başlangıca sıfırlanarak düzeltildi. `npm run build` ve `git diff --check` temiz.
- [x] **11. Sınıf Dörtgen Ayrıştırma Masası:** `MAT.11.2.1.1` ve `MAT.11.2.1.2` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci köşegen bıçağıyla dörtgeni iki üçgene ayırıyor; ilk görevde `180° + 180° = 360°`, ikinci görevde alan birleştiriciyle `T1 + T2 = dörtgen alanı` görünür hale geliyor. Browser Use görsel QA sırasında ikinci görevde bıçak ve alan raylarının üst üste binmesi, toplam alan haznesinin köşelere fazla yakın durması ve alan çiplerinin rastgele görünmesi yakalandı; ikinci görev tek alan birleştirme sahnesine sadeleştirildi. `npm run build`, `git diff --check`, completion ve console warning/error kontrolü temiz.
- [x] **11. Sınıf Logaritma Ters Ayna Odası:** `MAT.11.1.4.1`, `MAT.11.1.5.1` ve `MAT.11.1.5.2` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci `y=x` aynasını açarak üstel eğriyi logaritma grafiğine dönüştürüyor, sonra tabanı `a>1` ve `0<a<1` bölgelerine sürükleyerek artan/azalan logaritma davranışını görüyor. Browser Use ile yanlış onay, ayna sürükleme, taban sürükleme, üç görev zinciri, completion ekranı ve console warning/error kontrolü geçti. QA sırasında completion ekranının dar viewport'ta eski scroll konumuyla boş görünmesi yakalandı; `HighSchoolLabShell` görev/completion geçişlerinde scroll'u üste alacak şekilde düzeltildi. Log görevlerinin `a≈1` başlangıcında grafiği aşırı dikleştirmemesi için ara tabanlar pedagojik olarak yumuşatıldı. `npm run build` ve `git diff --check` temiz.
- [x] **11. Sınıf Korelasyon Serpilme Radarı:** `MAT.11.3.1.1`, `MAT.11.3.1.2` ve `MAT.11.3.1.3` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci nokta kapsüllerini serpilme radarında koordinat hedeflerine taşıyor; ardından aynı ana oyuncak pozitif ve negatif doğrusal ilişkiyi eğilim ışınıyla okutuyor. Browser Use ile yanlış onay, üç görev zinciri, completion ekranı ve console warning/error kontrolü geçti. Gemini 3 Flash ilk turda eksen/koordinat okunabilirliği ve eğilim ışını gücü için must-fix verdi; kontrast, punto ve ışın görünürlüğü düzeltildi, ikinci turda must-fix kalmadı. `npm run module:check`, `npm run build` ve `git diff --check` temiz.
- [x] **11. Sınıf Medya Korelasyon Denetçisi:** `MAT.11.3.2.1` kapsamı ayrı eleştirel veri okuryazarlığı deneyine dönüştürüldü. Öğrenci medya iddiasını veri izi, nedensellik alarmı ve güvenli sonuç mührüyle denetliyor; korelasyonun ilişki gösterebileceğini ama tek başına neden-sonuç kanıtı kurmadığını görüyor. Browser Use ile yanlış deneme, üç görev zinciri, completion ve console warning/error kontrolü geçti. Gemini 3 Flash must-fix olarak küçük metin/kontrast, sürgü etiketi ve eksen açıklamalarını yakaladı; kontrast, `Tarama/Mühür` etiketleri ve eksenler düzeltildi, son tur 92/100 PASS verdi. `npm run module:check`, `npm run build` ve `git diff --check` temiz.
- [x] **11. Sınıf Legacy Arşivi:** Eski `trig-pendulum`, `unit-circle`, `laser-defense` ve `slope-rollercoaster` modülleri silinmeden arşive alındı. Dashboard aktif 11. sınıf sayımı artık yalnız yeni standarttaki modülleri gösteriyor; eski deneyler `Eski Deneyler / Legacy` bölümünde route'ları korunarak listeleniyor. Browser Use ile 11. sınıf kapısının `1 GÖREV`, detay ekranının `Trigonometrik Osiloskop` + dört arşiv kartı şeklinde göründüğü ve console warning/error çıkmadığı doğrulandı.
- [x] **11. Sınıf Trigonometrik Kök Avcısı:** `MAT.11.1.2.x` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci sin/cos/tan/cot denklemlerinin köklerini 0°-360° dalga tünelinde iki kök işaretçisiyle hedef ışının kesişimlerine taşıyor; tan/cot görevlerinde tanımsızlık duvarları kök olmayan bölgeleri gösteriyor. Browser Use ile dar/embed eksen kopması ve sağ kırpılma yakalanıp düzeltildi; yanlış deneme, 4 görev zinciri, completion ve console warning/error kontrolü geçti. Gemini 3 Flash kapanışı 92/100 PASS, must-fix yok; `npm run module:check`, `npm run build` ve `git diff --check` temiz.
- [x] **11. Sınıf Richter-Desibel Ölçek Simülatörü:** `MAT.11.1.6.x` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci tek oran kadranıyla üstel büyüme, desibel ve Richter durumlarını aynı sahnede test ediyor; ham oran canlı büyüklük alanında görünürken sonuç alanı büyümede hesaplanan miktarı, desibel/Richter görevlerinde log ölçeğini okutuyor. Browser Use ile yanlış deneme, üç görev zinciri, completion ve console warning/error kontrolü geçti. Gemini 3 Flash ilk tur must-fixlerinden sonra büyüme başlığı, formül okunabilirliği ve oran vurgusu düzeltildi; final turu 94/100 PASS verdi. `npm run module:check`, `npm run build` ve `git diff --check` temiz.
- [x] **11. Sınıf Fonksiyon Bileşke Portları:** `MAT.11.1.7.1` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci `x=3` kapsülünü önce `g(x)=2x+1` makinesine, çıkan `g(x)=7` kapsülünü `f(u)=u²-4` portuna bağlayarak `f(g(3))=45` zincirini görüyor. Browser Use ile görsel smoke, yanlış onay AstroBot hatası, iki port bağlantısı, üç görev zinciri, completion ve console warning/error kontrolü geçti. Gemini 3 Flash final turu 96/100 PASS verdi, must-fix yok. QA sırasında sahne etiketi sadeleştirildi, zincir bandı kontrastı artırıldı ve AstroBot toast'ının sağ kontrol panelini kapatması düzeltildi. `npm run module:check`, `npm run build` ve `git diff --check` temiz.
- [x] **11. Sınıf Fonksiyon İşlem Mikseri:** `MAT.11.1.8.x` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci aynı `x` istasyonlarında `f(x)=x+3` ve `g(x)=x-2` akışlarını `+`, `-`, `×`, `÷` kadranından geçirerek yeni fonksiyon değerlerini görüyor; bölme görevinde `g(x)=0` noktası `kilit` güvenlik kapağına dönüşüyor. Browser Use ile görsel smoke, yanlış onay, `Home` fallback üzerinden dört işlem zinciri, completion ve console warning/error kontrolü geçti. İlk turda SVG kadranın `Home` fallback'i ve `f/g` etiket yakınlığı yakalandı; düzeltmeler sonrası Gemini 3 Flash final turu 96/100 PASS verdi, must-fix yok. `npm run module:check`, `npm run build` ve `git diff --check` temiz.
- [x] **11. Sınıf Özel Dörtgen Tanı Masası:** `MAT.11.2.2.x` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci özel dörtgenleri isim ezberiyle değil, kenar ve köşegen tarayıcılarından çıkan özellik izleriyle sınıflandırıyor; eşkenar dörtgende `4 kenar eşit + dik açı yok`, dikdörtgende `eşit köşegen + orta kesişim` ayrımı görünür. Browser Use ile kare yanlış kilit uyarısı, `Home` fallback üzerinden iki görev, completion ve console warning/error kontrolü geçti. Gemini 3 Flash ilk turda kare/eşkenar dörtgen kapsama must-fix'ini yakaladı; `dik açı yok` ayrımı eklendikten sonra final turu 98/100 PASS verdi. `npm run module:check`, `npm run build` ve `git diff --check` temiz.
- [x] **11. Sınıf Konkav-Konveks Lazer Dedektörü:** `MAT.11.2.3.x` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci lazer açı probuyla çokgeni tarıyor; konvekste tüm köşeler 180° altında kalırken konkav numunede içeri göçen köşe kırmızı `>180°` alarmı ve `266°` ölçüsüyle yakalanıyor. Browser Use ile yanlış kilitler, `Home` fallback üzerinden iki doğru görev, completion ve console warning/error kontrolü geçti. QA sırasında konkav görevde sağ panelin yanlışlıkla `90°` göstermesi yakalandı ve alarm açısına bağlandı. Gemini 3 Flash final turu 96/100 PASS verdi, must-fix yok. `npm run module:check`, `npm run build` ve `git diff --check` temiz.
- [x] **11. Sınıf Çokgen Köşegen ve Simetri Atölyesi:** `MAT.11.2.4.x` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci düzgün çokgenin kenar sayısını ayarlayarak köşegen lazerlerinin `n(n-3)/2` sayımını, dış açı yürüyüşünün 360° halkasını ve simetri aynasının eksen sayımını aynı kalibrasyon masasında görüyor. Browser Use görsel QA'da dar embed ray/etiket sıkışması ve hedef dili karışıklığı yakalandı; düzeltmelerden sonra Gemini 3 Flash final 98/100 PASS verdi. `npm run module:check`, `npm run build` ve `git diff --check` temiz.
- [x] **11. Sınıf Mozaik Kaplama Atölyesi:** `MAT.11.2.5.x` kapsamı ayrı kaplama deneyi olarak tamamlandı. Öğrenci altıgen ve üçgen fayansları mozaik masasında boşluk alarmı, 360° açı halkası ve hizalama rayı üzerinden kilitliyor. Browser Use ile yanlış deneme, `Home` fallback üzerinden üç görev, completion ve console warning/error kontrolü geçti. Gemini 3 Flash ilk turunda ray hitbox'ı, merkez etiket çakışması ve mühür dili must-fix çıktı; ray büyütüldü, pasif etiketler gizlendi, mühür kontrolleri ayrıştırıldı ve completion auto-scroll eklendi. Final Gemini 98/100 PASS, must-fix yok; 11. sınıf yeni üretim kuyruğu tamamlandı.
- [x] **10. Sınıf Ters Fonksiyon Aynası:** `MAT.10.2.5.x` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci `y=x` aynasında giriş/çıkış kapsüllerini ters portlara taşıyor; doğrusal terslemede rol değişimini, karesel/karekök katmanında tek dal gerekliliğini ve rasyonel terslemede yasak duvarın korunmasını görüyor. Browser Use ile yanlış deneme, `Home` fallback üzerinden üç görev, completion ve console warning/error kontrolü geçti. Gemini 3 Flash ilk turda rasyonel görevde kapsül/makine sıkışması ve ayna etiketi çakışma riskini must-fix verdi; kapsüller dış portlara açıldı, etiket rozet yapıldı. Final Gemini 100/100 PASS, must-fix yok.
- [x] **10. Sınıf İşaret Tablosu Tarayıcısı:** `MAT.10.2.6.x` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci kök duraklarını sabit `x=2`, `x=3` hedef lazerlerine hizalıyor; ardından `>0` için dış pozitif bantları, `<0` için iç negatif bandı kilitliyor. Browser Use ile yanlış deneme, kök sürükleme, pozitif/negatif bant zinciri, completion ve console `[]` geçti. Gemini 3 Flash ilk turda kök çakışma belirsizliği ve eşitsizlik akışının ilk görevde yeterince görünmemesi must-fix çıktı; ayrı sol/sağ raylar, `çakışmaz boşluk`, sürekli akış etiketi ve güçlü hedef lazerleri eklendi. Final Gemini 96/100 PASS, must-fix yok. `npm run module:check -- sign-table-scanner`, `npm run build` ve `git diff --check` temiz.
- [x] **10. Sınıf Sabit Alan Üçgen Rayı:** `MAT.10.4.3.x` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci tepe noktasını tabana paralel rayda sola, sağa ve orta mühür noktasına taşıyarak taban ve yüksekliğin değişmediği durumda üçgen alanının sabit kaldığını görüyor. Browser Use ile yanlış deneme, `Home` fallback üzerinden üç görev, completion ve console `[]` doğrulandı. Gemini 3 Flash ilk turda sağ hedefte sol yön başarı mesajını must-fix verdi; başarı dili ray/alan büyüklüğü odağına çekildi, etiket/formül dili güçlendirildi. Final Gemini 96/100 PASS, must-fix yok. `npm run module:check -- constant-area-triangle-rail`, `npm run build` ve `git diff --check` temiz.
- [x] **10. Sınıf Sinüs-Kosinüs Arazi Ölçeri:** `MAT.10.4.4.x` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci dik olmayan arazi üçgeninde kosinüs lensiyle iki kenar ve aradaki açıdan eksik `BC` kenarını, sinüs lensiyle bilinen açı-kenar çiftinden bilinmeyen `B` açısını ölçüyor; rapor görevi iki teoremin kullanım şartını mühürlüyor. Browser Use ile yanlış deneme, `Home` fallback üzerinden üç görev, completion ve console `[]` geçti. Gemini 3 Flash finali 96/100 PASS verdi, must-fix yok; toast/buton yakınlığı should-fix olarak nonblocking kaldı. `npm run module:check -- sine-cosine-terrain-surveyor`, `npm run build` ve `git diff --check` temiz.
- [x] **10. Sınıf Birim Çember Kalkanı:** `MAT.10.4.2.1` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci birim çember açı düğümünü hedeflere taşıyor; yatay `cos x` ve dikey `sin x` izdüşümlerinin kare plakaları merkez kalkan çekirdeğinde `sin²x + cos²x = 1` toplamını koruyor. Browser Use ile yanlış deneme, `Home` fallback üzerinden cos²/sin²/kalkan mührü görevleri, completion ve console `[]` geçti. Gemini 3 Flash 92/100 PASS verdi, must-fix yok; güncel AstroBot yerleşimiyle IAB smoke tekrarlandı. `npm run module:check -- unit-circle-identity-shield`, `npm run build` ve `git diff --check` temiz.
- [x] **10. Sınıf Asal Kilit Kasası:** `MAT.10.1.1.1` ve `MAT.10.1.1.2` kapsamı ayrı makro deney olarak tamamlandı. Öğrenci `30` sayısını asal lazerlerle kırıyor, sonra tam bölen rafını kilitliyor. Browser Use ile yanlış deneme, `Home` fallback üzerinden asal lazer ve tam bölen görevleri, completion ve console `[]` geçti. Gemini 3 Flash ilk turunda cevabı söyleyen ipucu ve AstroBot toast yerleşimi must-fix çıktı; ipuçları yönlendirici dile çekildi, sonuç/metrik kartları kilitlenmeden gizlendi ve toast küçük/orta embed'de üst konuma alındı. Final Gemini 92/100 PASS, must-fix yok. `npm run module:check -- prime-lock-vault`, `npm run build` ve `git diff --check` temiz.
- [x] **10. Sınıf EBOB/EKOK Dişli Kutusu:** `MAT.10.1.2.x` kapsamı tamamlandı. Öğrenci `12 = 2² · 3` ve `18 = 2 · 3²` için EBOB küçük ortak kuvvetleri ve EKOK büyük birleşik kuvvetleri aynı dişli kutusunda kilitliyor. Browser Use final QA ve Gemini 3 Flash 96/100 PASS geçti.
- [x] **10. Sınıf Kalan Kapısı:** `MAT.10.1.3.1` kapsamı tamamlandı. Öğrenci bölme yapmadan rakam toplamı, son basamak ve son iki basamak lensleriyle kalan tokenlarını kilitliyor. Browser Use final QA ve Gemini 3 Flash 95/100 PASS geçti.
- [x] **10. Sınıf Sayma Montaj Hattı:** `MAT.10.3.1.x` kapsamı tamamlandı. Çarpma yoluyla sayma ve toplama yoluyla sayma ayrımı montaj hattında `6` ve `7` sonuçlarıyla mühürlendi. Browser Use final QA ve Gemini 3 Flash 96/100 PASS geçti.
- [x] **10. Sınıf Cebir Algoritma Makinesi:** `MAT.10.3.2.1` kapsamı tamamlandı. `x=3` girdisi işlem boru hattından geçip `7` çıktısına ve Türkçe sözde kod mührüne bağlandı. Browser Use final QA ve Gemini 3 Flash 98/100 PASS geçti.
- [x] **10. Sınıf Çapraz Tablo Dedektifi:** `MAT.10.6.x` kapsamı tamamlandı. Çapraz tablo hücresi, beklenen değer sapması ve medya yanlılığı raporu tek dedektif sahnesinde kapandı. Browser Use final QA ve Gemini 3 Flash 96/100 PASS geçti.
- [x] **10. Sınıf Koşullu Olasılık Filtresi:** `MAT.10.7.1.1` kapsamı tamamlandı. Çift gelen zar koşulu örnek uzayı daraltıyor, `4'ten büyük` hedefi yeni uzayda `1/3` olarak mühürleniyor. Browser Use final QA ve Gemini 3 Flash 98/100 PASS geçti.
- [x] **10. Sınıf Bağımlı Çekiliş Makinesi:** `MAT.10.7.2.1` kapsamı tamamlandı. İlk kırmızı top geri konmadan çıkarılıyor, yeni örnek uzay `2 kırmızı / 4 toplam` oluyor ve ikinci kırmızı olasılığı `1/2` olarak kilitleniyor. Browser Use final QA ve Gemini 3 Flash 94/100 PASS geçti.

---

## 📉 Sistem Sağlığı Raporu (Code Reviewer)
*Verim İzleme Metrikleri:*
- **Derleme (Build):** Herhangi bir TypeScript veya çökme hatası YOK. Tam performans.
- **Mimari:** "AstroBot" ve bazı HUD modülleri yavaş yavaş izole edildi. Gelecekte diğer modüller de bu merkezi kütüphaneye geçirilebilir. Modül sayısı arttıkça performansta sorun yaşanmıyor.
