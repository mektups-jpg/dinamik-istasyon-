# Module Showcase Readiness

Bu dosya Kaptan'ın kalite etiketlerini ayrı tutar: omurga üretimi ile vitrine çıkacak premium modül aynı şey değildir.

## Etiketler

- `Skeleton`: Route ve kabuk var; gerçek öğrenme akışı yok.
- `Playable`: Görev zinciri oynanır; öğrenci anlamlı feedback alır.
- `Rework Needed`: Çalışsa bile görsel, pedagojik veya etkileşim kalitesi yetersiz.
- `Showcase Candidate`: Production seviyesine yaklaşan aday; canlı QA ve teknik kapılar temizdir ama Kaptan görsel onayı bekler.
- `Showcase Ready`: Kaptan onayı, canlı QA, responsive kontrol ve Gemini must-fix temizliği birlikte geçmiştir.

## İlkokul 1. Sınıf

| Modül | Etiket | Kanıt | Not |
| --- | --- | --- | --- |
| 01-06 Renkli Paket Tablosu | `Showcase Ready` | Browser/Codex canlı QA: yanlış/doğru akış, 5 görev zinciri, completion, tekrar oynama, dashboard görünümü ve taşma kontrolü geçti; 2026-06-10 dil cilasında soru kökü `hangi satıra gider?` yerine `hangi kutuya koyarsın?` çizgisine çekildi; `module:check` 20 pass / 3 warn / 0 fail; `tsc`, `build`, `git diff --check` geçti | 2026-06-10 Kaptan canlı görsel onayıyla `Showcase Ready / Vitrin Hazır`. |

## İlkokul 2. Sınıf

| Modül | Etiket | Kanıt | Not |
| --- | --- | --- | --- |
| 02-06 Kalibrasyon Kulesi | `Showcase Candidate` | Browser/Codex canlı QA: yanlış/doğru akış, 7 görev zinciri, completion, tekrar oynama, dashboard görünümü ve taşma kontrolü geçti; `module:check` 27 pass / 3 warn / 0 fail; `tsc`, `build`, `git diff --check` geçti | İlk canlı turda yanlış yöne bakan saat kolu düzeltildi. Kaptan canlı görsel onayı bekler; onay gelmeden `Showcase Ready` yapılmaz. |
| 02-07 Veri Çekirdeği | `Showcase Candidate` | Browser/Codex canlı QA: yanlış/doğru akış, 5 görev zinciri, completion, tekrar oynama, dashboard görünümü ve taşma kontrolü geçti; `module:check` 22 pass / 3 warn / 0 fail; `tsc`, `build`, `git diff --check` geçti | Çokluk tahmini, çetele, grafik farkı ve sıralama görevleri 2. sınıf diliyle oynanır durumda. Kaptan canlı görsel onayı bekler; onay gelmeden `Showcase Ready` yapılmaz. |
| 02-08 Kasa Terminali | `Showcase Candidate` | Browser/Codex canlı QA: yanlış/doğru akış, 5 görev zinciri, completion, tekrar oynama, dashboard görünümü, buton sığma ve taşma kontrolü geçti; `module:check` 19 pass / 3 warn / 0 fail; `tsc`, `build`, `git diff --check` geçti | Para toplamı ve para üstü görevleri büyük fiziksel para görselleriyle oynanır durumda. Kaptan canlı görsel onayı bekler; onay gelmeden `Showcase Ready` yapılmaz. |
| 02-09 Eş Gruplar Kargo Bandı | `Showcase Ready` | Browser/Codex canlı QA: yanlış kart `Bir daha bak` feedback'i verdi, 5 görev doğru zincir completion'a ulaştı, `Tekrar Oyna` 1/5'e döndürdü ve ilk görev değişti, `Ana Merkez` ilkokul hattına döndü, dashboard görünümü ve taşma kontrolü geçti; 2026-06-10 dil/pedagoji cilasında `dron`, `kısa işlem` ve teknik `kısa yol` ağırlığı azaltıldı; 2026-06-10 sayı çeşitliliğinde ilk 20 seed için 20 farklı görev imzası doğrulandı; canlı ipucu yarım `25 - 5 - 5 ...` yerine tam `25 -> 20 -> 15 -> 10 -> 5 -> 0` yoluna çekildi; `module:check` 22 pass / 3 warn / 0 fail; `tsc`, `build`, `git diff --check` geçti | Eş kutu, çarpma işlemi, eşit paylaştırma, tur sayısı ve diziliş karşılaştırma görevleri 2. sınıf diliyle oynanır durumda. 2026-06-10 Kaptan canlı görsel onayıyla `Showcase Ready / Vitrin Hazır`. |
| 02-10 Eksik Sayı Terazisi | `Showcase Ready` | Browser/Codex canlı QA: yanlış kart `Bir daha bak` feedback'i verdi, 5 görev doğru zincir completion'a ulaştı, `Tekrar Oyna` 1/5'e döndürdü ve ilk görev değişti, dashboard görünümü ve taşma kontrolü geçti; 2026-06-10 dil cilasında çocuk ekranındaki `Kuantum/kilit/küp/yuva` dili azaltıldı, başlangıcı bul sahnesinin yük modeli düzeltildi, sağ panel soru tekrarı kısa eşitliğe indirildi, kefe içi `+4` gibi sayı etiketi kaldırıldı, görev havuzu 8 örneğe çıkarıldı; `module:check` 21 pass / 3 warn / 0 fail; `tsc`, `build`, `git diff --check` geçti | Eksik toplanan, eksilen, çıkan ve iki işlemli eşitlik görevleri 2. sınıf diliyle oynanır durumda. 2026-06-10 Kaptan canlı görsel onayıyla `Showcase Ready / Vitrin Hazır`. |
| 02-11 Şekil Tamamlama Atölyesi | `Showcase Ready` | Browser/Codex canlı QA: yanlış kart `Bir daha bak` feedback'i verdi, 4 görev doğru zincir completion'a ulaştı; atom artık yalnız robot/araç modelindeki soluk taslak parçanın adını seçtiriyor, küre hacimli top görseline çekildi, eski simetri/örüntü/kroki görevleri çocuk ekranından çıkarıldı; `module:check` 19 pass / 3 warn / 0 fail; `tsc`, `build`, `git diff --check` geçti | Cisim/düz şekil model kurma tek ana fikre indirildi. Eski ad 2. sınıf için soyut bulunduğu için görünen ad `Şekil Tamamlama Atölyesi` yapıldı. Simetri, örüntü ve kroki `Simetri Aynası`, `Kroki Yol Bulucu`, `Örüntü Tamir Bandı` olarak ayrı atomlarda planlandı. 2026-06-11 Kaptan canlı görsel onayıyla `Showcase Ready / Vitrin Hazır`. |

## İlkokul 3. Sınıf

| Modül | Etiket | Kanıt | Not |
| --- | --- | --- | --- |
| 03-06 Sütun Grafiği Terminali | `Showcase Ready` | Browser/Codex canlı QA: yanlış/doğru akış, 5 görev zinciri, completion, tekrar oynama, dashboard görünümü, buton sığma ve taşma kontrolü geçti; 2026-06-03 tekrar kontrolünde 6 oyun turu tamamlandı, 3 farklı veri seti görüldü, console app issue yok; `module:check` 20 pass / 3 warn / 0 fail; `tsc`, `build`, `git diff --check` geçti | Tablo, sütun grafiği, en çok/en az ve grafikten karar görevleri 3. sınıf diliyle oynanır durumda. 2026-06-03 Kaptan canlı görsel onayıyla `Showcase Ready / Vitrin Hazır`. |
| 03-07 Dört İşlem Makinesi | `Showcase Ready` | Browser/Codex canlı QA: yanlış kart `Bir daha bak` feedback'i verdi, 13 görev doğru zincir completion'a ulaştı, `Tekrar Oyna` 1/13'e döndürdü ve ilk görev değişti; son dashboard kontrolünde `/?grade=3` içinde `Vitrin Hazır` göründü; 2026-06-06 bozma cevap sızıntısı QA: `52 - 18` görevinde doğru seçim öncesi hamle ve dönüşüm sonucu saklandı, doğru karttan sonra dönüşüm açıldı, yanlış/doğru feedback çalıştı, yatay taşma ve console app issue yok; 2026-06-06 tahmin cila QA: `Yuvarlama modeli` artık yuvarlanmış sayıyı veya yaklaşık toplamı yazmıyor; sayı iki yüzlük arasında nokta olarak görünüyor; 2026-06-03 layout cila QA: `Kısa ipucu` makinenin üstüne taşındı, AstroBot ve modelle çakışma yok; 2026-06-03 dil cila QA: problem ekranlarında `3 kutuda dörder kalem var. 5 kalem daha eklendi` doğrulandı; 2026-06-06 taşıma cila QA: statik `Taşıma kutusu` kaldırıldı; 2026-06-06 kalan modeli QA: pasif `Kalan kutusu` yerine tam gruplar ve dışarıda kalan nesneleri gösteren `Kalan modeli` geldi, sahne cevap metnini yazmadan seçim yaptırıyor; 2026-06-06 sayı çeşitliliği QA: iki tam turda 13/13 completion ve 13 ifadenin 13'ünde yeni sayılar doğrulandı; 2026-06-06 completion metni QA: sağ panelde eski adlandırma yerine `Dört İşlem Makinesi tamam.` doğrulandı; `module:check` 41 pass / 3 warn / 0 fail; `tsc`, `build`, `git diff --check` geçti | Yuvarlama, zihinden işlem, eldeli/bozmalı işlemler, çarpma, kalanlı bölme, çok adımlı problem ve eşitlik görevleri 3. sınıf diliyle oynanır durumda. Tahmin sahnesi sayıları yüzlük çizgisi üzerinde gösteriyor; yuvarlanmış sonucu yazmıyor, toplamı ve doğru kartı öğrenci düşünüp seçiyor. Genel/pasif `İşlem parçaları`, statik `Taşıma kutusu` ve pasif `Kalan kutusu` kaldırıldı; yalnız `Yuvarlama modeli`, seçilen sayıya göre çalışan `Bozma modeli` ve cevabı yazmadan grupları gösteren `Kalan modeli` kaldı. Problem dili `dörder` ve `eklendi` ifadeleriyle net. `Tekrar Oyna` tüm görev sayılarını yeniler. İpucu artık makinenin üstünde sakin bir rehber şeridi. 2026-06-06 Kaptan canlı görsel onayıyla `Showcase Ready / Vitrin Hazır`. |
| 03-08 Birim Dönüşüm İstasyonu | `Archived` | Önceki canlı QA: yanlış/doğru feedback, 12 görev completion, tekrar oynama, console ve taşma kontrolü geçti | 2026-06-07 pedagojik kararla fazla geniş bulundu. Aktif akış `Saati Okuyorum`, `Zaman Dönüşüm Makinesi`, `Süre Tahmin Dedektifi`, `Ölçü Birimi Makinesi`, `Para Değeri Atölyesi` ve `Litre Ölçme Atölyesi` atomlarına ayrıldı. |
| 03-08a Saati Okuyorum | `Showcase Ready` | Browser/Codex canlı QA: yanlış kart kırmızı feedback + AstroBot ipucu verdi, 4 görev completion, console 0 warning/error, taşma yok; 2026-06-07 çeşitlilik QA: saat havuzu 12 okunabilir örneğe çıkarıldı, `Tekrar Oyna` sonrası ilk saat `12:00` -> `05:55` değişti; `module:check` 13 pass / 4 warn / 0 fail; `tsc`, `build`, `git diff --check` geçti | Analog saat ve dijital saat kodu tek odakta; aynı saat ezberi riski kapatıldı. 2026-06-07 Kaptan canlı görsel onayıyla `Showcase Ready / Vitrin Hazır`. |
| 03-08b Zaman Dönüşüm Makinesi | `Showcase Ready` | Browser/Codex canlı QA: yanlış kart kırmızı feedback + AstroBot ipucu verdi, 4 görev completion, console 0 warning/error, taşma yok; 2026-06-07 çeşitlilik QA: 8 farklı seed dizisi doğrulandı, `Tekrar Oyna` sonrası ilk cevap `480 dakika` -> `60 dakika` değişti; `module:check` 13 pass / 4 warn / 0 fail; `tsc`, `build`, `git diff --check` geçti | Saat-dakika ve dakika-saniye dönüşümleri tek odakta; aynı küçük sayı döngüsü ezberi riski kapatıldı. 2026-06-07 Kaptan canlı görsel onayıyla `Showcase Ready / Vitrin Hazır`. |
| 03-08c Süre Tahmin Dedektifi | `Showcase Ready` | Browser/Codex canlı QA: yanlış kart kırmızı feedback + AstroBot ipucu verdi, 3 görev completion, console 0 warning/error, taşma yok; 2026-06-07 cila QA: canlı modelde `saniye`, `dakika`, `saat` süre ölçeği doğrulandı; 2026-06-07 çeşitlilik QA: olay havuzu 9 örneğe çıkarıldı, 8 seed için 8 farklı görev dizisi doğrulandı, canlı akışta `Tekrar Oyna` sonrası ilk olay değişti; 2026-06-07 dil QA: `Okulda geçirilen yarım gün` kaldırıldı, `Okulda geçen bir sabah` havuzda doğrulandı; `module:check` 12 pass / 4 warn / 0 fail; `tsc`, `build`, `git diff --check` geçti | Günlük süre tahmini tek odakta. Canlı model süre birimi ölçeğini düşündürür; doğru cevabı yazmaz. Tekrar oynama farklı günlük olaylar getirerek ezber riskini azaltır. `Yarım gün` yerine okul sabahı dili kullanılır. 2026-06-07 Kaptan canlı görsel onayıyla `Showcase Ready / Vitrin Hazır`. |
| 03-08d Ölçü Birimi Makinesi | `Showcase Candidate` | Browser/Codex canlı QA: yanlış kart kırmızı feedback + AstroBot ipucu verdi, 4 görev completion, console 0 warning/error, taşma yok; `module:check` 13 pass / 4 warn / 0 fail; `tsc`, `build`, `git diff --check` geçti | Metre-santimetre ve kilogram-gram tek odakta. Kaptan canlı görsel onayı bekler; onay gelmeden `Showcase Ready` yapılmaz. |
| 03-08e Para Değeri Atölyesi | `Showcase Candidate` | Browser/Codex canlı QA: yanlış kart kırmızı feedback + AstroBot ipucu verdi, 4 görev completion, console 0 warning/error, taşma yok; `module:check` 13 pass / 4 warn / 0 fail; `tsc`, `build`, `git diff --check` geçti | Kuruş-TL ve para toplamı tek odakta. Kaptan canlı görsel onayı bekler; onay gelmeden `Showcase Ready` yapılmaz. |
| 03-08f Litre Ölçme Atölyesi | `Showcase Candidate` | Browser/Codex canlı QA: yanlış kart kırmızı feedback + AstroBot ipucu verdi, 5 görev completion, console 0 warning/error, taşma yok; `module:check` 14 pass / 4 warn / 0 fail; `tsc`, `build`, `git diff --check` geçti | Litre, mililitre ve 1 litreye tamamlama tek odakta. Kaptan canlı görsel onayı bekler; onay gelmeden `Showcase Ready` yapılmaz. |
| 03-09 Dijital Geometri Tahtası | `Showcase Candidate` | Browser/Codex canlı QA: ilk ekranda çivili tahta ve cevap kartları görünür, yatay/dikey SVG çizgi filtresi canlı kontrolde düzeltilip tekrar doğrulandı, yanlış kart `Bir daha bak` feedback'i verdi, 10 görev doğru zincir completion'a ulaştı, `Tekrar Oyna` 1/10'a döndürdü, dashboard `/?grade=3` içinde `Görüş Gerekli` göründü; `module:check` 26 pass / 4 warn / 0 fail; `tsc`, `build`, `git diff --check` geçti | Cisim özellikleri, çokgen adı, sanal cetvel, çevre hesabı ve simetri görevleri 3. sınıf diliyle oynanır durumda. Kaptan canlı görsel onayı bekler; onay gelmeden `Showcase Ready` yapılmaz. |

## İlkokul 4. Sınıf

| Modül | Etiket | Kanıt | Not |
| --- | --- | --- | --- |
| 04-01 Altı Basamaklı Sayı Dedektifi | `Showcase Ready` | Browser/Codex canlı QA: yanlış/doğru akış, 5 görev zinciri, completion, tekrar oynama, ana merkeze dönüş, taşma yok; `module:check` 28 pass / 3 warn / 0 fail; genel `tsc`, `build`, `git diff --check` kapısı geçti | Kaptan canlı görsel onayıyla vitrin hazır olarak kilitlendi. |
| 04-02 İşlem Kısayolları Atölyesi | `Showcase Ready` | Browser/Codex canlı QA: yanlış/doğru akış, 7 görev zinciri, completion, tekrar oynama, ana merkeze dönüş, taşma yok; `module:check` 28 pass / 3 warn / 0 fail; genel `tsc`, `build`, `git diff --check` kapısı geçti | Kaptan canlı görsel onayıyla vitrin hazır olarak kilitlendi. |
| 04-03 Kesir Denge Terazisi | `Showcase Ready` | Browser/Codex canlı QA: yanlış/doğru akış, 9 görev zinciri, completion, tekrar oynama, ana merkeze dönüş, taşma yok; `module:check` 29 pass / 3 warn / 0 fail; genel `tsc`, `build`, `git diff --check` kapısı geçti | Çokluğun kesrini bul görevinde tam sayı cevap doğrulandı; tam sayılı kesir görevinde eşdeğer bileşik kesir çeldiricisi kaldırıldı. Kaptan canlı görsel onayıyla vitrin hazır olarak kilitlendi. |
| 04-04 Açınım ve Çevre Keşfi | `Showcase Ready` | Browser/Codex canlı QA: yanlış/doğru akış, 6 görev zinciri, completion, tekrar oynama, ana merkeze dönüş, cevap kartı taşması yok; `module:check` 26 pass / 3 warn / 0 fail; genel `tsc`, `build`, `git diff --check` kapısı geçti | Çevre seçenek tekrarı ve dış yol görseli cila sonrası tekrar doğrulandı; açınım soru dili "hangi cismin açınımıdır" olarak netleştirildi; küp açınımı havuzu 6 geçerli varyanta, simetri görevi 6 ayna varyantına çıkarıldı; eş kenar görevi yalnız iki parlak kenar çizgisiyle netleştirildi; kod deseni havuzu 6 desene çıkarıldı ve tekrar oynayınca aynı desen üst üste gelmiyor. Kaptan canlı görsel onayıyla vitrin hazır olarak kilitlendi. |
| 04-05 Açı Ölçme Çarkı | `Showcase Ready` | Browser/Codex canlı QA: yanlış/doğru akış, 5 görev zinciri, completion, tekrar oynama, ana merkeze dönüş, taşma yok; `module:check` 23 pass / 3 warn / 0 fail; genel `tsc`, `build`, `git diff --check` kapısı geçti | Dönme, iletki, dik/dar/geniş açı akışı büyük menteşe görseliyle anlaşılır durumda. Kaptan canlı görsel onayıyla vitrin hazır olarak kilitlendi. |
| 04-06 Ölçü Dönüşüm Makinesi | `Showcase Ready` | Browser/Codex canlı QA: yanlış/doğru akış, 4 görev zinciri, completion, tekrar oynama, taşma yok, console app issue yok; `module:check` 21 pass / 4 warn / 0 fail; genel `tsc`, `build`, `git diff --check` kapısı geçti | Eski karma istasyondan ayrıldı; 2026-06-02 Kaptan canlı görsel onayıyla `Showcase Ready / Vitrin Hazır`. |
| 04-07 Olasılık Etiketleri | `Showcase Ready` | Browser/Codex canlı QA: yanlış/doğru akış, 3 görev zinciri, completion, tekrar oynama, taşma yok, console app issue yok; `module:check` 20 pass / 4 warn / 0 fail; genel `tsc`, `build`, `git diff --check` kapısı geçti | İmkânsız, kesin ve olabilir kararları ayrı atomda sadeleşti; 2026-06-02 Kaptan canlı görsel onayıyla `Showcase Ready / Vitrin Hazır`. |
| 04-08 İki Sınıf Grafiği | `Showcase Ready` | Browser/Codex canlı QA: yanlış/doğru akış, 4 görev zinciri, completion, tekrar oynama, taşma yok, console app issue yok; `module:check` 18 pass / 4 warn / 0 fail; genel `tsc`, `build`, `git diff --check` kapısı geçti | İki veri grubu grafiği ölçü ve olasılıktan ayrıldı; 2026-06-02 Kaptan canlı görsel onayıyla `Showcase Ready / Vitrin Hazır`. |
| 04-09 Bölme Tahmini | `Showcase Ready` | Browser/Codex canlı QA: tahmin ekranında kalan/kalanlı dil görünmedi, sonuç havuzu yalnız 10'un katı kalmadı (`70`, `45`, `25` gözlendi), yanlış kart `Bir daha bak` feedback'i verdi, 3 doğru zincir completion'a ulaştı, `Tekrar Oyna` 1/3'e döndürdü, dashboard `/?grade=4` içinde ayrı kartı göründü, console app issue yok; `module:check` 18 pass / 4 warn / 0 fail; genel `tsc`, `build`, `git diff --check` kapısı geçti | Yalnız `MAT.4.2.3.2` kapsamını taşır; 2026-06-03 Kaptan canlı görsel onayıyla `Showcase Ready / Vitrin Hazır`. |
| 04-10 Bölme Dedektifi | `Showcase Ready` | Browser/Codex canlı QA: tahmin/yaklaşık dil görünmedi, 2 görevli kalanlı/kalansız bölme akışı yanlış kartta `Bir daha bak` feedback'i verdi, doğru zincir completion'a ulaştı, `Tekrar Oyna` yeni bölme göreviyle 1/2'ye döndürdü, dashboard `/?grade=4` içinde ayrı kartı göründü, console app issue yok; `module:check` 19 pass / 4 warn / 0 fail; genel `tsc`, `build`, `git diff --check` kapısı geçti | Yalnız kalanlı ve kalansız bölme algoritmasını taşır; 2026-06-03 Kaptan canlı görsel onayıyla `Showcase Ready / Vitrin Hazır`. |
| 04-11 Uzun Çarpma Fabrikası | `Showcase Ready` | Browser/Codex canlı QA: öğrenci dili `İkinci çarpan olan ...` ve `İkinci çarpan burada ... demektir` çizgisine çekildi; yanlış kart `Bir daha bak` feedback'i verdi, doğru kart görev geçişi sağladı, 3 görev completion'a ulaştı, dashboard `/?grade=4` içinde `Vitrin Hazır` göründü, console app issue yok; `module:check` 18 pass / 4 warn / 0 fail; genel `tsc`, `build`, `git diff --check` kapısı geçti | Uzun çarpma birler/onlar adımlarıyla ayrı çocuk-dostu akışta kaldı. 2026-06-03 Kaptan canlı görsel onayıyla `Showcase Ready / Vitrin Hazır`. |
| 04-12 Problem Kur ve Çöz Atölyesi | `Showcase Ready` | Browser/Codex canlı QA: Türkçe hikaye dili `6 kutuda 8'er kalem var. Kutulara 15 kalem daha kondu...` netliğine çekildi; eski `kalem daha geldi` kalıbı görünmedi, yanlış kart `Bir daha bak` feedback'i verdi, doğru kart görev ilerletti, 3 görev doğru zincir completion'a ulaştı, `Tekrar Oyna` yeni problem/işlem dizisiyle 1/3'e döndürdü, console app issue yok; `module:check` 19 pass / 4 warn / 0 fail; genel `tsc`, `build`, `git diff --check` kapısı geçti | Karma problem çözme ve problem tasarımı ayrı stüdyo akışı oldu. 2026-06-03 Kaptan canlı görsel onayıyla `Showcase Ready / Vitrin Hazır`. |

## 12. Sınıf

| Modül | Etiket | Kanıt | Not |
| --- | --- | --- | --- |
| 12-01 Dizi Çarkı | `Showcase Ready` | Computer Use canlı akış, iki yanlış deneme, 6 görev zinciri, reset, dizi motoru, canlı projeksiyon, 6 görevli özel completion ekranı, Playwright state screenshotları, Gemini final v2 `92/100 PASS`, 2026-05-17 Kaptan canlı görsel onayı | Her atom iki örnekle pekişiyor; completion 3 ustalık kartında 6 kanıt satırı gösteriyor. CU sırasında yakalanan `+5` görevinde motorun `+3` göstermesi düzeltildi. Kaptan onayı: "Dizi çarkı modülü güzel oldu." Dashboard etiketi `Vitrin Hazır`. |
| 12-02 Polinom Kasası | `Showcase Ready` | Computer Use desktop açılış/yanlış/doğru/reset/completion, 2026-05-16 son cila full-chain CU kontrolü, 2026-05-17 Browser canlı sadeleştirme kontrolü, Playwright `00/25/50/75/100` screenshot paketi, Gemini 3.1 Pro final2 `92/100 PASS`, `module:check`, `tsc`, `build`, 2026-05-17 Kaptan canlı görsel onayı | İlk Gemini turu `65/100 FAIL` verdi; sahne fiziksel kasa derinliği ve hareketli tarama merceğiyle yeniden işlendi. Son cilada mercek seçimi yalnız önizleme, doğru/yanlış hükmü yalnız `Kasayı Kilitle` sonrası olacak şekilde cevap sızıntısı kapatıldı. 2026-05-17'de 5 mercek korunup sabit seçenek sırası + 3 odak + 2 yardımcı hiyerarşisine çekildi; yalnız karar rafı parlıyor. Kaptan onayı: "polinom kasası iyi oldu" ve "polinom kasası adlı modül de hazır oldu." Dashboard etiketi `Vitrin Hazır`. |
| 12-03 İşaret Yörünge Radarı | `Showcase Ready` | Computer Use desktop açılış/yanlış/doğru/reset/completion, 2026-05-16 son cila full-chain CU kontrolü, 2026-05-17 Browser canlı işaret tablosu cila kontrolleri, Playwright `00/25/50/75/100` screenshot paketi, Gemini 3.1 Pro final `100/100 PASS`, `module:check`, `tsc`, `build`, `git diff --check`, 2026-05-17 Kaptan canlı görsel onayı | İlk Gemini turu `95/100 PASS` olsa da yasak nokta ayrımı için `MUST_FIX` verdi; pembe yasak istasyonu ve `!` rozeti eklendi. Son cilada tarayıcı seçimi yalnız önizleme, doğru/yanlış hükmü yalnız `Yörüngeyi Kilitle` sonrası olacak şekilde cevap sızıntısı kapatıldı. Aralık/işaret rozetleri dikey ve ortalı hale getirildi. Kaptan onayı sonrası dashboard etiketi `Vitrin Hazır`. |
| 12-04 Limit Asimptot Sensörü | `Showcase Ready` | Computer Use desktop açılış/yanlış/doğru/reset, 2026-05-16 cila: CU yerel cebir sızıntı tespiti + Playwright 1488x768 başlangıç/önizleme/doğru zincir/sonsuz katsayı/0-0/completion/reset, console temiz, Gemini 3.1 Pro final `94/100 PASS`, `module:check`, `tsc`, `build`, `git diff --check`, 2026-05-19 Kaptan canlı görsel onayı | İlk Gemini turu `35/100 FAIL` verdi; grafik/eğri eksikliği ve statik 0/0 sahnesi gerçek koordinat grafiği, yaklaşma sensörleri, asimptot çizgisi ve çarpan vinciyle rework edildi. Son cilada seçme/test ayrımı kapatıldı: sonuç değerleri ve yanlış alarmı yalnız `Limiti Kilitle` sonrası görünür. 0/0 cila turunda AstroBot balonuna kesir çizgili denklem eklendi, sahne ve sağ panel kalabalıklaştırılmadı. Kaptan onayı sonrası dashboard etiketi `Vitrin Hazır`. |
| 12-05 Süreklilik Köprüsü | `Showcase Ready` | Computer Use desktop açılış/yanlış/doğru/eksik `f(a)`, Playwright final cila başlangıç/seçim-önizleme/yanlış/doğru/eksik pim/sıçrama/yanlış yükseklik/final/completion screenshot paketi, console temiz, Gemini 3.1 Pro final `95/100 PASS`, `module:check`, `tsc`, `build`, 2026-05-19 Kaptan canlı görsel onayı | İlk Gemini `40/100 FAIL`, ikinci `75/100 FAIL`; letterbox/detached UI, üst üste label ve sıçrama limit etiketi düzeltildi. Son cila turlarında seçim/test ayrımı korundu, `lim` notasyonu hizalandı, `f(2)` dili dinamikleştirildi ve matematiksel anlamı olmayan dekoratif sarı destek çizgileri kaldırıldı. Kaptan onayı sonrası dashboard etiketi `Vitrin Hazır`. |
| 12-06 Türev Eğim Sürücüsü | `Showcase Ready` | Computer Use desktop reset/yanlış/doğru zincir/completion, Playwright final-pass başlangıç/yanlış/sekant/teğet/final/completion screenshot paketi, console temiz, Gemini 3.1 Pro rehab final `100/100 PASS`, `module:check`, `tsc`, `build`, `git diff --check`; 2026-05-16 rehab: Computer Use ile sahne üstü eğim probu ve kalıcı AstroBot balonu kontrol edildi; 2026-05-22 Kaptan canlı görsel onayı | İlk Gemini `85/100 PASS` olsa da 1488x768 sağ panel taşması için `MUST_FIX` verdi; panel sıkılaştırıldı. Kaptan feedback'i sonrası native sürüklenebilir eğim probu eklendi. Son cila turunda çift görünen prob tutacağı tek native sürgüye indirildi, sahne içi feedback kartı AstroBot ile çakıştığı için kaldırıldı ve AstroBot giriş gecikmesi kısaltıldı. Sivri uç/kopuk pist yoğunluğu ayrı Türev Yok Alarm İstasyonu'na ayrıldı; kesen-teğet omurgası Kaptan onayıyla kilitlendi. Dashboard etiketi `Vitrin Hazır`. |
| 12-06b Türev Yok Alarm İstasyonu | `Showcase Ready` | Browser/Codex desktop canlı QA: sahne probu doğrudan SVG üstünde sürüklenince slider `50 -> 92`, iki görev doğru zincirle completion, console `0 warning / 0 error`; sistem screenshot `.agent/visual-reports/12-06b-polish-completion-system.png`; `module:check` 18 pass / 2 warn / 0 fail, `tsc`, `build`, `git diff --check`; 2026-05-23 Kaptan canlı görsel onayı | Sivri uç ve kopuk grafik türev-yok nedenleri ayrı sahnelerde kilitleniyor. Erken `f'(2) yok` cevabı kaldırıldı; sahne önce `tek teğet?` / `kesintisizlik?` sorusunu gösteriyor, gerçek hüküm yalnız doğru alarm sonrası açılıyor. Sağ panel destek/fallback seviyesinde kaldı; ana oyuncak sahne üstü sürükleme. Kaptan onayıyla dashboard etiketi `Vitrin Hazır`. |
| 12-07 Türev Kural Dökümhanesi | `Review Needed` | Computer Use desktop açılış/yanlış/doğru zincir/bölüm/zincir/completion/reset, 2026-05-16 CU yanlış `Çarpım Kolu` önizleme/test alarmı, Playwright 1488x768 yanlış/toplam/fark/çarpım/bölüm/zincir/completion/reset, 2026-05-23 Browser/Codex eksik parça alarmı + sahne parça kilidi + beş görev completion + console temiz, Gemini 3.1 Pro final `100/100 PASS`, `module:check`, `tsc`, `build`, `git diff --check` | Kartuş seçimi artık başarı için yetmiyor; öğrenci sahnede `türevle/koru/topla/çıkar/g² zırh/dış-iç` parçalarını kilitlemeden formül ve çıktı açılmıyor. Çizgi kalabalığı giderildi, completion genişliği sahneye çekildi. Kaptan onayı olmadan `Showcase Ready` yapılmayacak. |
| 12-08 Optimizasyon Arazisi | `Review Needed` | Computer Use desktop açılış/yanlış önizleme/yanlış alarm/ilk doğru kilit, Playwright başlangıç/yanlış/artış/azalış/ekstremum/maksimum/minimum/completion/dar dizüstü akışı, console temiz, Gemini 3.1 Pro final `95/100 PASS`, `module:check`, `tsc`, `build`, `git diff --check` | İlk Gemini turu `85/100 PASS` olsa da ortadaki yönerge metni pistle çakıştığı için `MUST_FIX` verdi; metin kaldırıldı. Son cila karar seçiminin cevap sızdırmasını kapattı; gerçek formül/sonuç ve gizli kanıt yalnız `Araziyi Test Et` sonrası açılıyor. Kaptan onayı olmadan `Showcase Ready` yapılmayacak. |
| 12-09 Çember Radar İstasyonu | `Review Needed` | Computer Use desktop açılış/yanlış/doğru kesen-kiriş-teğet-yay-çevre açı-merkez açı-alan zinciri/completion/reset, Playwright başlangıç/yanlış/kiriş/teğet/çevre açı/alan/completion/dar dizüstü screenshot paketi, console temiz, Gemini 3.1 Pro final `95/100 PASS`, `module:check`, `tsc`, `build`, `git diff --check` | İlk Gemini final turu kontrast ve İngilizce completion etiketi için `MUST_FIX` verdi; kontrast, Türkçe completion etiketi ve feedback padding'i düzeltildi. Kaptan onayı olmadan `Showcase Ready` yapılmayacak. |
| 12-10 Katı Cisim Ölçüm Dökümhanesi | `Review Needed` | Computer Use desktop başlangıç/yanlış/doğru hacim gözlemi, Playwright tam 10 görev zinciri, 00/25/35/40/50/60/65/75/80/85/88/90/100/dar dizüstü screenshot paketi, WebGL pixel check `20/20`, console temiz, Gemini 3.1 Pro final `95/100 PASS`, `module:check`, `tsc`, `build`, `git diff --check` | Hacim iç dolum, yüzey alanı dış zırh olarak 3D sahnede ayrışıyor. Gemini yalnız prizma yüzey formül dili için `SHOULD_FIX` verdi; `A = 2Tₐ + Yₐ` olarak düzeltildi. Kaptan onayı olmadan `Showcase Ready` yapılmayacak. |
| 12-11 Büyük Veri Yargı Laboratuvarı | `Review Needed` | Computer Use desktop başlangıç/yanlış önizleme/yanlış alarm, Playwright başlangıç/yanlış/doğru ilk görev/medyan/IQR/trend/completion/1488x768 ve erken cevap sızıntısı kontrolü, console temiz, Gemini 3.1 Pro final `95/100 PASS`, `module:check`, `tsc`, `build`, `git diff --check` | Veri kaynağı, filtre, grafik çekirdeği ve güvenli sonuç cümlesi tek veri yargı hattında ayrışıyor. Son cila `Kilit 1` rozeti, nötr kaynak/yargı önizlemeleri ve test sonrası `ONAY/RED` diliyle seçim-test ayrımını düzeltti. Kaptan onayı olmadan `Showcase Ready` yapılmayacak. |
